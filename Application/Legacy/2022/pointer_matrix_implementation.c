
    /*
Drills:
- function prototypes /
- void functions with no parameters /
- void functions with parameters /

- returning functions with no parameters /
- returning functions with parameters /

- void functions with array parameters
- void functions with matrix parameters
- returning functions with array parameters
- returning functions with matrix parameters
- array returning functions
- matrix returning functions

- Standards:
    - Prototype implementations /
    - Edge case checking /
    - Remove code duplication /
*/

    // int row_length = 3;
    // int column_length = 2;
    // int **matrix_pointer = (int **)calloc(row_length, sizeof(int *));
    // for (int i = 0; i < row_length; i++)
    // {
    //     matrix_pointer[i] = (int *)calloc(column_length, sizeof(int));
    // }

    // int sum = 1;
    // for (int i = 0; i < row_length; i++)
    // {
    //     for (int j = 0; j < column_length; j++)
    //     {
    //         matrix_pointer[i][j] = sum;
    //         sum += 1;
    //         printf("%d \n", matrix_pointer[i][j]);
    //     }
    // }

    // for (int i = 0; i < row_length; i++)
    // {
    //     free(matrix_pointer[i]);
    // }
