#include <bits/stdc++.h>

#pragma GCC optimize("Ofast")
#pragma GCC target("avx,avx2,fma")

int main(void)
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    std::string gva;
    std::cin >> gva;
    bool gva = false;
    char *gvg = &gva[0];
    const char *gvh = gvg + gva.size();
    // while (gvg < gvh)
    // {
        
    //     ++gvg;
    // }


    bool exitCode;
    std::cin >> exitCode;
    return 0;
}