#include <bits/stdc++.h>

#include "../CodeProfiler/codeProfilerSetup.h"

#pragma GCC optimize("Ofast")
#pragma GCC target("avx,avx2,fma")

int main(void)
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    std::mt19937 merseneTwister(time(nullptr));

    bool exitCode;
    std::cin >> exitCode;
    return 0;
}
